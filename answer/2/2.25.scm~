#lang sicp

(define (choose1 items)
  (cdr (cdr (car items))))

(define (choose2 items)
  (car (car items)))

(define (choose3 items)
  (cdr (cdr (cdr (cdr (cdr (cdr items)))))))

(choose1 '(1 3 (5 7) 9))
(choose2 '((7)))
(choose3 '(1 (2 (3 (4 (5 (6 7)))))))